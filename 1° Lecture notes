# Prices are computed monthly. Seasonally adjusted means that, for example, we want to remove the deviation of Christmas shopping (inflation) or August less shopping (deflation).
# On FRED data we can modify data, for example modify the frequency
